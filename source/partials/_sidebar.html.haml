.sidebar
  %ul
    %li
      %h2 Categories
      %ul
        - blog.categories.each do |k, v|
          %li=link_to titleize(humanize(k)), "/blog/#{k}.html"
    %li
      %h2 Tags
      %div
        - blog.tags.each do |k, v|
          =link_to k, "/blog/tags/#{k}.html"
